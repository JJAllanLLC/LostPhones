# STAGING ONLY: this robots.txt MUST be removed or conditioned safely
# before any future production launch.
# Pair with vercel.json X-Robots-Tag: noindex, nofollow, noarchive
# (also staging-only and mandatory to remove or condition before production).
User-agent: *
Disallow: /
